# Node
node_modules/
npm-debug.log*
*.log

# Dart / Flutter
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
*.iml
.idea/
ios/Pods/
ios/.symlinks/
ios/Flutter/Flutter.framework
ios/Flutter/Flutter.podspec

# Secrets / local
*.pem
*.p12
.env
.env.*
fixtures.json
*.provisioned.csv
cameras.csv

# OS
.DS_Store
Thumbs.db

# Dart package (not app) — don't pin the lock
pubspec.lock

# ...but the TS app's crypto/frame golden vectors ARE a committed test asset
!app/src/bridge/__fixtures__/fixtures.json

# Expo (native projects generated by `expo prebuild` in CI, not committed)
.expo/
app/ios/
app/android/
app/expo-env.d.ts
